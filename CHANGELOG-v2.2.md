# 心光 v2.2 更新日志

## v2.2.0 (2026-08-20)

### 加密体系修复与增强（A 系列）

- **历史文件迁移加密**：启用端到端加密后，可一键将云端已有的明文 `.md` 笔记（含根目录、`journal/`、`brain/` 子目录）逐个加密回写，并提供进度显示与统计（新加密 / 已加密 / 旧格式 / 失败明细）
- **加密设置失败自动回滚**：启用加密过程中任一环节失败时，自动恢复进入前的全部本地与云端状态，不再留下"半成品"配置
- **警告文案修正**：明确区分"恢复密钥备份文件"与"笔记真实密文"，避免误以为坚果云网页端看到的内容是乱码备份
- **加密状态三态显示**：`已启用（会话已解锁 / 未解锁）`、`云端已加密 · 本浏览器未解锁`、`未启用`，状态一目了然
- **云端备份双通道**：恢复密钥同时用恢复码与加密密码两通道包裹备份到坚果云。清除浏览器数据、换设备或使用无痕窗口后，输入原加密密码即可恢复，**无需再担心加密开关"被重置"**
- **防误覆盖保护**：本地无密钥但云端检测到加密备份时，禁止直接生成新密钥覆盖，强制走密码 / 恢复码恢复流程，防止旧密文永久无法解密

### 登录与认证修复（B 系列）

- **修复 iOS 登录按钮永久锁定**：登录 Promise 链补齐 `.catch()` 与 `.finally()`，异常时按钮与加载状态正确复位
- **网络错误不再误报为"密码错误"**：`verifyPassword` 返回细分错误类型（网络 / 服务器 / 认证 / 限流），提示语与实际情况一致
- **登录限流**：按 IP 限制 15 分钟内最多 10 次尝试，超出后暂时锁定，抵御暴力破解
- **密码哈希存储**：登录密码改为 PBKDF2（加盐）哈希后落盘，首次登录自动从旧明文格式升级，无需用户操作
- **API 鉴权中间件（可选开关）**：环境变量 `XINGUANG_API_AUTH=on` 开启后，除豁免端点外所有 `/api/*` 请求需携带登录时下发的 `x-auth-token`（有效期 24 小时）。**默认关闭，行为与 v2.1 完全一致**

### 系统与网络安全（C 系列）

- **替换危险防火墙脚本**：删除会关闭防火墙的 `test-firewall.bat` 与 `add-firewall-3001.bat`，新增 `setup-firewall.bat`——防火墙全程保持开启，仅对 3001 端口添加"专用网络"入站规则，公司 / 公共 WiFi 环境自动免疫
- **端口占用检测跨平台**：非 Windows 平台端口被占时给出明确提示（`lsof -i :端口`）后退出，不再尝试仅限 Windows 的自动释放

### 代码质量（D 系列）

- **filesmd 端点重构**：消除重复的文件读写代码，改为并发读取，加载更快
- **敏感词过滤修正**：改为针对系统提示词的特征串匹配，消除对正常内容的误伤
- **构建代码分割**：`vendor-react`（141.7 kB）等拆分为独立 chunk，加快移动端首屏加载
- **项目名规范化**：`package.json` 的 `name` 更正为 `xinguang`
- **单元测试**：新增 Vitest 测试套件（`npm test`），覆盖加密往返、恢复码校验、云端备份兼容、旧格式识别、历史文件迁移等 19 个用例
- **TypeScript 5.8.3 兼容**：修复 `tsconfig.json` 中已移除的 `baseUrl` 选项导致的构建失败

---

## v2.2 vs v2.1 关键对比

### 端到端加密可靠性

| 场景 | v2.1 | v2.2 |
|------|------|------|
| 清除浏览器数据 / 无痕窗口后 | ❌ 加密开关重置，需重新设置（旧密文无法读取的风险） | ✅ 输入原密码即可从云端备份恢复 |
| 云端已有的明文历史笔记 | ❌ 保持明文 | ✅ 一键迁移加密 + 进度统计 |
| 启用加密中途失败 | ❌ 留下半成品配置 | ✅ 自动回滚到初始状态 |
| 换设备首次使用 | ❌ 只能靠恢复码 | ✅ 密码或恢复码双通道恢复 |
| 误设新密码覆盖旧密钥 | ❌ 可能发生，旧数据永久失读 | ✅ 检测到云端备份即拦截 |

### 登录与安全

| 项目 | v2.1 | v2.2 |
|------|------|------|
| iOS 登录失败后按钮 | ❌ 可能永久锁定 | ✅ 正常复位可重试 |
| 网络异常提示 | ❌ 误报"密码错误" | ✅ 区分网络/服务器/认证/限流 |
| 登录密码存储 | ❌ 明文 | ✅ PBKDF2 加盐哈希（自动升级） |
| 暴力破解防护 | ❌ 无 | ✅ IP 限流 15 分钟 10 次 |
| API 未授权访问 | ❌ 局域网内可直调 | ✅ 可选令牌鉴权（默认关闭，零影响） |

### 防火墙脚本

| 脚本 | v2.1 | v2.2 |
|------|------|------|
| `test-firewall.bat` | ❌ 关闭防火墙（高危） | ✅ 已删除 |
| `add-firewall-3001.bat` | ❌ 全网络放行 | ✅ 已删除 |
| `setup-firewall.bat` | — | ✅ 仅专用网络放行，防火墙保持开启 |

### 工程质量

| 项目 | v2.1 | v2.2 |
|------|------|------|
| 单元测试 | ❌ 无 | ✅ 19 个加密核心用例（`npm test`） |
| 首屏加载 | 单一 bundle | vendor 拆分，react 独立 chunk |
| 类型检查 | `tsc` 报错（baseUrl 已移除） | ✅ 通过 |
| Lint 新增问题 | — | 0（与 v2.1 基线持平） |

---

## 升级说明

1. **直接覆盖升级**：`npm install && npm run build`，无需数据迁移
2. **首次登录**：旧明文密码自动升级为哈希存储，用户无感知
3. **加密用户**：如之前在无痕窗口丢失加密开关，升级后在坚果云配置页输入原加密密码即可恢复（云端需存在 `.xinguang_recovery.json` 备份）
4. **启用加密后**：建议在配置页执行一次"历史文件迁移加密"，将云端明文笔记全部加密
5. **API 鉴权**：默认关闭；若局域网内存在不可信设备，启动前设置 `XINGUANG_API_AUTH=on`
