# 心光 v2.1 更新日志

## v2.1.0 (2026-08-19)

### 安全修复

- **移除登录页默认密码显示**：登录页面不再显示默认密码 `xinguang2026`，避免屏幕泄露
- **修复密钥导出参数**：`deriveAESKey` 的 `extractable` 参数从 `false` 改为 `true`，修复登录密码验证在某些环境下的崩溃问题
- **凭据智能加密存储**：加密会话解锁时，坚果云凭据自动加密落盘，不再明文存储
- **断开连接时清除会话密钥**：断开坚果云连接时立即清除内存中的主密钥

### Bug 修复

- **修复加密凭据时所有坚果云操作失败**：全面异步化凭据获取，解决读/写/删除/列表等操作在加密场景下返回"未配置账号"的问题
- **修复 `hasCredentials()` 误判**：加密凭据时正确识别为"已连接"状态
- **修复旧格式文件迁移失败**：`migrateOldToNewFormat` 现在支持加密凭据场景
- **修复用户名回填为空**：加密凭据时设置页面正确显示用户名

### 新增功能

- `logout()` — 显式登出清除会话密钥
- `saveCredentialsSmart()` — 根据会话状态自动选择加密/明文存储
- `getCredentialUsername()` — 从加密/明文凭据中统一提取用户名
- `hasEncryptedCredentials()` — 查询凭据是否已加密存储

### 架构改进

- 凭据获取全面异步化：从同步 `getCredentials()` 切换为 `getCredentialsAsync()`，使加密凭据的自动解密在所有数据通路中生效

---

## v2.1 vs v1.20.1 详细对比

### 问题背景

v1.20.1 引入了主密钥加密架构（Master Key Architecture），但在实现上遗留了多个关键 bug，导致加密功能在实际使用中大量失效。v2.1 是一次聚焦于修复这些问题的增量升级。

### 关键 Bug 对比

| Bug | v1.20.1 | v2.1 | 影响 |
|-----|---------|------|------|
| `extractable: false` | ❌ `exportKey` 崩溃 | ✅ 修复 | 登录密码验证失败 |
| 加密凭据时坚果云操作 | ❌ 全部失败 | ✅ 修复 | 读/写/删除/列表不可用 |
| `hasCredentials()` 判断 | ❌ 误判为未连接 | ✅ 修复 | UI 显示错误 |
| 旧格式迁移 | ❌ 静默失败 | ✅ 修复 | 数据迁移不完整 |
| 用户名回填 | ❌ 空白 | ✅ 修复 | 用户体验差 |

### 安全增强对比

| 功能 | v1.20.1 | v2.1 |
|------|---------|------|
| 登录页显示默认密码 | ❌ 明文显示 | ✅ 已删除 |
| 坚果云凭据存储 | ❌ 始终明文 | ✅ 智能加密 |
| 断开连接清除密钥 | ❌ 不清除 | ✅ 立即清除 |

### 新增功能对比

| 功能 | v1.20.1 | v2.1 |
|------|---------|------|
| `logout()` | ❌ 无 | ✅ 有 |
| `saveCredentialsSmart()` | ❌ 无 | ✅ 有 |
| `getCredentialUsername()` | ❌ 无 | ✅ 有 |
| `hasEncryptedCredentials()` | ❌ 无 | ✅ 有 |

### 结论

**v2.1 是 v1.20.1 的必要升级**。v1.20.1 的主密钥架构设计正确，但实现上有 5 个 bug 导致加密凭据场景下大量功能失效。v2.1 通过全面异步化凭据获取、修复密钥导出参数、增强安全存储，使加密功能真正可用。

**建议使用 v2.1 作为生产版本。**
