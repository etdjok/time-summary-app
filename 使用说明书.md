# 心光 - 使用说明书

## 一、项目简介

心光是一款个人日常管理系统，支持待办事项、时间记录、笔记管理等功能，具备离线访问、多设备同步、密码保护等特性。

---

## 二、访问方式

### 方式一：局域网访问（推荐，在家使用）

**适用场景：** 所有设备在同一 WiFi 下

**访问地址：** `http://192.168.3.3:3001`

**登录密码：** `xinguang2026`（可在设置中修改）

**优点：**
- 速度快，无需外网
- 稳定可靠

**注意事项：**
- 小米6必须保持开机并连接 WiFi
- 小米6的 IP 地址如果变化，访问地址也要跟着改

---

### 方式二：ngrok 外网访问（出门使用）

**适用场景：** 手机使用 4G/5G，或不在同一 WiFi 下

**如何选择访问方式：**

不需要在应用里切换，直接用不同的地址访问即可：

| 场景 | 访问地址 | 说明 |
|------|---------|------|
| 在家（同一WiFi） | `http://192.168.3.3:3001` | 速度快，稳定 |
| 出门（不同网络） | ngrok 生成的地址 | 需要先启动 ngrok |

**首次配置（只需做一次）：**

1. 注册 ngrok 账号：https://ngrok.com/signup
2. 登录后在 Dashboard 找到「您的 Authtoken」，点击复制
3. 在小米6的 Termux 中运行（粘贴你的 token）：
```bash
~/ngrok config add-authtoken 你的token
```

**每次出门使用步骤：**

1. 确保服务器在运行（如果没运行）：
```bash
cd ~/time-summary-app
node api/server.js
```

2. 新开一个 Termux 会话（点击底部 ≡ 菜单 → New Session），运行：
```bash
~/ngrok http 3001 --basic-auth "admin:你的密码"
```
- `admin` 是 ngrok 的用户名（固定）
- `你的密码` 是 ngrok 的访问密码，建议和心光登录密码保持一致

3. ngrok 会显示一个外网地址，类似：
```
https://xxxx.ngrok-free.app
```

4. 在任何设备的浏览器中访问该地址

5. 先输入 ngrok 的用户名和密码（admin / 你的密码）

6. 再输入心光的登录密码

**ngrok 密码说明：**

- ngrok 密码是保护外网地址的第一道门
- 在启动命令中设置：`--basic-auth "admin:你的密码"`
- 每次启动时可以设不同的密码
- 建议和心光登录密码保持一致，方便记忆
- 修改密码只需下次启动时用新密码即可，无需额外配置

**停止 ngrok：**

按 `Ctrl + C` 即可停止

**优点：**
- 任何网络都能访问（4G/5G/其他WiFi）
- 双重密码保护（ngrok 密码 + 心光密码）
- 基于 ngrok 全球网络，连接稳定

**缺点：**
- 免费版地址每次重启会变（需重新获取）
- 需要小米6保持开机和联网
- 需要外网能访问 ngrok 服务器

---

## 三、密码管理

### 修改密码

1. 登录心光应用
2. 点击右上角 `<>` 图标（分类管理）
3. 向下滚动，点击「修改密码」
4. 输入当前密码、新密码、确认新密码
5. 点击「确认修改」

**重要：** 修改密码后，所有设备都需要用新密码重新登录。

### 默认密码

`xinguang2026`

---

## 四、服务器管理

### 启动服务器

在小米6的 Termux 中运行：
```bash
cd ~/time-summary-app
node api/server.js
```

看到 `服务器运行在 http://localhost:3001` 即成功。

### 停止服务器

在 Termux 中按 `Ctrl + C`，或运行：
```bash
pkill -f node
```

### 开机自动启动

已配置开机自动启动脚本，小米6重启后服务器会自动运行。

---

## 五、功能说明

### 5.1 快速记录
- 收集：快速记录想法
- 待办：创建待办事项
- 想法：记录灵感
- 日记：写日记
- 笔记：记录笔记

### 5.2 时间周期
- 支持日、周、月、季度、半年、年视图
- 可按时间周期查看和统计数据

### 5.3 视图切换
- **列表视图：** 传统列表展示
- **四象限矩阵：** 按紧急×重要维度分类任务
- **热力图：** 日历热力图展示任务分布

### 5.4 编辑功能
- 发送后的消息可以继续编辑
- 可以重新分类
- 可以修改优先级和状态

### 5.5 离线支持
- PWA 支持，可安装到桌面
- Service Worker 缓存核心资源
- 离线操作队列，网络恢复后自动同步

### 5.6 坚果云同步
- 支持连接坚果云 WebDAV
- 自动同步 files.md 笔记

---

## 六、常见问题

### Q1: 手机无法访问
- 检查小米6是否开机并连接 WiFi
- 检查小米6的 IP 地址是否变化
- 检查服务器是否在运行

### Q2: 密码错误
- 确认使用的是最新密码
- 如果忘记密码，在小米6的 Termux 中运行：
```bash
echo '{"password":"xinguang2026"}' > ~/time-summary-app/api/password.json
```
- 然后重启服务器

### Q3: ngrok 地址变了
- 每次重启 ngrok 地址会变
- 重新运行 ngrok 命令获取新地址

### Q4: 数据丢失
- 数据存储在浏览器本地（localStorage）
- 建议定期备份
- 可使用坚果云同步重要笔记

---

## 七、技术架构

- **前端：** React + TypeScript + Vite
- **后端：** Node.js + Express
- **部署：** 小米6 + Termux
- **穿透：** ngrok（可选）
- **存储：** localStorage + 坚果云 WebDAV

---

## 八、版本历史

- **v1.0：** 基础待办事项功能
- **v1.1：** PWA 离线支持、可视化功能
- **v1.2：** 每日固定任务、编辑功能
- **v1.3：** 四象限、热力图、登录密码
- **v1.4：** 服务器端统一密码管理

---

*最后更新：2026年7月26日*
